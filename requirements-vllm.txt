# KSAgent vLLM 推理服务依赖
# 用途：单独部署 Qwen3-VL OpenAI 兼容 API（生产默认端口 8004）
# 环境：CUDA 12.x + Python 3.10+
#
# 安装：
#   pip install -r requirements-vllm.txt
#
# 启动示例：
#   vllm serve Qwen3-VL-4B-Instruct-FP8 --port 8004 --host 0.0.0.0
#
# 与 KSAgent 同机部署时，torch / transformers 版本须与 requirements.txt 一致，勿分别升级。

# 核心推理引擎
vllm==0.11.0

# PyTorch（与 requirements.txt / 生产环境对齐）
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==0.36.2

# vLLM 性能与分布式
triton==3.4.0
xformers==0.0.32.post1
ray==2.55.1
accelerate==1.13.0
einops==0.8.2

# 结构化输出 / 约束解码
outlines_core==0.2.11
llguidance==0.7.30
lm-format-enforcer==0.11.3
partial-json-parser==0.2.1.1.post7
xgrammar==0.1.25

# 量化与模型格式
compressed-tensors==0.11.0
gguf==0.19.0

# Qwen3-VL 多模态
qwen-vl-utils==0.0.14
opencv-python-headless==4.13.0.92
av==17.0.1
pillow==12.2.0

# 监控（vLLM OpenAI API 服务）
prometheus_client==0.25.0
prometheus-fastapi-instrumentator==7.1.0

# vLLM 运行时
tiktoken==0.13.0
mistral_common==1.11.2
openai-harmony==0.0.8
depyf==0.19.0
msgspec==0.21.1
pyzmq==27.1.0
cupy-cuda12x==14.1.0
cuda-pathfinder==1.5.5
setproctitle==1.3.7

# NVIDIA CUDA 12 运行时（与生产环境对齐，pip 亦会自动拉取）
nvidia-cublas-cu12==12.8.4.1
nvidia-cuda-cupti-cu12==12.8.90
nvidia-cuda-nvrtc-cu12==12.8.93
nvidia-cuda-runtime-cu12==12.8.90
nvidia-cudnn-cu12==9.10.2.21
nvidia-cufft-cu12==11.3.3.83
nvidia-cufile-cu12==1.13.1.3
nvidia-curand-cu12==10.3.9.90
nvidia-cusolver-cu12==11.7.3.90
nvidia-cusparse-cu12==12.5.8.93
nvidia-cusparselt-cu12==0.7.1
nvidia-nccl-cu12==2.27.3
nvidia-nvjitlink-cu12==12.8.93
nvidia-nvtx-cu12==12.8.90
